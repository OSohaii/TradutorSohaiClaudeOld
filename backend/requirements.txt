fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
httpx==0.28.1
google-genai==0.3.0
python-multipart==0.0.20
python-dotenv==1.0.1
# Pillow is required by the Ichigo provider to normalize the pixel-space
# bounding boxes returned by ichigoreader.com to the 0-1000 schema used by
# the rest of the pipeline. Without it, Ichigo translation silently produced
# bubbles with bad coordinates.
Pillow==11.0.0

# Test
pytest==8.3.4
pytest-asyncio==0.24.0
httpx[http2]==0.28.1
